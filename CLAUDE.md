# Project Instructions

## Comment style

Code comments must only explain what the code does/how it works — never the
change/decision behind it. Do not add comments narrating edit history, the
reasoning for a fix, or infrastructure/business context that motivated a
change (e.g. "X lives in a different VPC, so we use agent Y" is decision
narration, not a description of the code itself).
